# Mission：独立研究并实时模拟跨场所永续价差

## 为什么做

目标不是从群聊复制一段代码或一个价差数字，而是独立完成以下工作：从当前市场清单发现候选，先用低成本事实淘汰假机会，再用真实盘口、完整成本、执行失败和新数据判断是否值得进入实时模拟。

NautilusTrader 是历史重放与实时运行框架，不替代市场规则研究和交易经济学判断。

## Day21 的完成标准

用户能够运行一个命令行研究系统，并在未见过的永续候选上完成：

1. 扫描 Lighter 与 Hyperliquid 的全部当前永续市场。
2. 根据上市状态、经济对象、交易时段、价格来源、最小订单和数据质量淘汰无效候选。
3. 对剩余候选计算两种方向的共同可成交数量、盘口成交均价和完整进出场成本。
4. 分别评估两腿主动成交，以及一腿挂单成交、另一腿主动补对冲。
5. 用不可变事件重放部分成交、拒单、行情过期、断连和重启。
6. 使用相同研究逻辑完成 NautilusTrader 确定性历史重放和实时模拟。
7. 给出“拒绝”“继续采集”或“仅模拟”决定，并指出证据尚未证明什么。
8. 用户能预测结果、审查关键实现、修改一条真实规则并诊断一个注入故障。

所有候选被拒绝仍可完成课程。可靠地证明当前没有机会，比制造正收益结果更重要。

## 研究对象的定义

- **严格套利**：收益能由确定结算规则、原子执行或同一交易过程锁定，不依赖未经证明的未来收敛。
- **相对价值候选**：价差可能收敛，但存在持有时间、资金费、价格来源和退出风险。Lighter 与 Hyperliquid 的跨场所永续默认属于这一类。
- 在市场身份、共同数量、完整成本和退出机制通过前，只称为“候选”，不称为“机会”。

## 当前范围

- 实现两个执行场所：Lighter、Hyperliquid。
- 扫描两边全部永续市场，不把市场列表或候选写死。
- 程序允许增加更多场所，但 Day21 前不实现第三个执行适配器。
- BTC、ETH 用于验证工程和成本计算；现实世界资产（RWA）用于验证交易时段、外部/内部定价和价格恢复规则。
- 主研究时间尺度为 5 秒至 30 分钟。
- 系统自动淘汰并排名，最终候选由用户人工选择。
- 只做命令行工具和人工值守的实时模拟，不建设网页、无人值守交易或复杂告警平台。

## 学习方式

- 助手先完成主要实现，用户不被迫从空文件开始。
- 课程先展示当前市场数据、关键代码和技术解释，再选择一个最能暴露理解差异的动作验收。
- 解释结果、审查代码、修改规则和诊断故障按当天问题择一或组合，不作为每天必须重复的固定仪式。
- 哈希、清单和学习记录服务于研究复现，不作为教学内容或掌握证明。
- 网页按钮、选择题、阅读完成和助手生成的测试不能证明用户掌握。
- 重要知识必须在新市场或新故障中再次使用。

## 运行与资金限制

- Day12–21 不发送真实订单，不配置 NautilusTrader 执行客户端。
- 允许只读查询现有 Lighter、Hyperliquid 账户的费率、余额、持仓、保证金和资金费记录。
- 密钥不写入仓库、课程、日志或容器镜像。
- 东京服务器用于连续运行公开行情采集器 3–7 天；采集进程设置磁盘上限、日志轮转和自动恢复。
- 后续人工值守实验总资金不超过 `$150`；`$30` 是事故上限，不是日常风险预算。真实订单仍需新的明确授权。

## Day21 之后

1. 将 AX Exchange 模拟环境作为第三个 RWA 场所迁移测试，先核验账户准入、整数合约和资金要求。
2. 连续实时模拟至少 7 天并完成账户只读核验后，再讨论测试环境下单。
3. 链上交易重构、跨链路径和本地分叉重放（fork）作为独立后续课程，不与当前永续/RWA 系统同时建设。
