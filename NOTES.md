# 教学与研究工作区备注

## 用户能力与偏好

- Python、SQL 可独立完成；Linux、Docker 会使用；能阅读 WebSocket 和测试代码，但本课程不要求用户编写或修改代码。
- Agent 负责实现、运行、修复和维护；用户通过真实候选训练发现现象、提出解释、选择验证、解读证据和迁移研究方法。
- 默认使用中文。只有接口字段、代码标识符和没有准确中文替代的术语保留英文，首次出现时说明含义。
- 工作日投入 30–45 分钟；周末可投入 1–2 小时。浏览器工作台展示真实数据、图表、候选和实验结果，Telegram 用于讨论和下研究指令。
- 课程不使用浅选择题、固定预测模板、哈希、页面完成状态或 Agent 测试通过证明掌握。

## 已确认的目标

- 核心产出是可持续运行的市场机会研究工作台、可复用工具与研究方法；盈利验证是应用目标，不是每天重复否定候选。
- Day14–21 是 8 个系统里程碑，可以跨多个工作日，不因日期虚报完成。
- 主工作台扫描 Lighter 与 Hyperliquid 全部可映射永续；核心领域逻辑不把两家场所写死。
- WTI/Brent 是 RWA、时段、展期和 oracle 状态的基准案例，不是唯一研究范围。
- 主时间尺度为 5 秒至 30 分钟；资金费和时段机制可以使用更长窗口。
- 同时保留交易吸引力榜与研究价值榜；候选数量按当天真实质量决定，最多优先展示 3 个，不为了凑数制造候选。
- 机会雷达探索其他 perpetual venue、RWA/TradeFi、CEX–DEX、资金费和链上方向；达到最低证据标准后再接入主工作台。
- 先完成主动成交基线，后续研究一腿挂单、另一腿主动补对冲和双腿失败。
- 没有高质量候选时，使用历史真实异常或可复现故障增强工作台，而不是重复 `unknown/exclude` 问答。

## 当前能力缺口

- Day16 v2 已建立共同经济敞口、冻结 L2 走档、部分成交阻断、同敞口四次主动成交基线与容量下界（`execution_engine.py` + `/workbench/execution`）；工作台模块按职责命名并固定使用浅色模式；全仓 151 项测试通过。
- 需要把账户实际费率（HL HIP-3 `userFees`、Lighter 账户层级）、指定持有期 funding、未来退出 L2 和账户可用保证金接入，才能形成完整现金区间；当前同盘口往返只是成本基线，不是未来退出预测。
- RWA external/internal 状态、展期权重和外部时段仍需独立证据，不能从单个 `oraclePx` 推断。
- 双腿部分成交、第二腿延迟、旧订单风险和补对冲需要在事件回放中训练（Day19）。
- NautilusTrader 目前未作为本仓库运行依赖锁定；引入前必须固定版本和 Python `>=3.12,<3.15` 环境，并分别验证 Lighter/Hyperliquid 只读数据适配器。

## 数据、账户与服务器授权

- 允许在用户的东京服务器部署公开行情只读采集器，连续运行 3–7 天。
- 允许使用现有 Lighter、Hyperliquid 密钥只读核验账户费率、余额、持仓、保证金和资金费记录。
- 不发送订单，不签署交易；密钥只从运行环境读取并默认脱敏。
- 服务器地址、账户信息和密钥不写入课程材料。

## 当前入口

- 学习目标：`MISSION.md`
- 当前唯一计划：`resource/plan.md`
- 里程碑索引：`curriculum/README.md`
- 当前里程碑：`curriculum/day16.md`
- 工作台设计：`research/design/workbench-design.md`

## 旧材料处理决定

- Day14 旧同步快照页面和代码仅保留为数据质量诊断素材，不作为学习完成证明或后续页面模板。
- 保留有来源的原始数据、仍可复现的研究代码、研究笔记和历史学习记录。
- 历史学习记录只证明当时做过相应活动，不自动证明用户已经掌握。
