# 教学工作区备注

## 用户教学偏好

- 每次开始新的 `teach` 内容前，先简要介绍：当前学习目标、21 天课程规划、当前进度、后续规划。
- 教学顺序优先采用：具体数据/场景 → 用户用自己的话解释 → 只纠正关键错误 → 新场景迁移验证。
- 避免连续模板填空和重复要求定义；主动回忆应简洁，并服务于真实理解。

## 课程状态基线

- 课程主计划：`resource/plan.md`（v6）。
- Day 1–7 lesson、reference、assets、lab 和 learning records 已建立；Day 7 JavaScript 交互故障已修复，提交并 push 为 `55a18c4`。
- 用户下一步需要重新完成 Day 7 迁移验收；随后进入 Day 8 的跨场所数据字段和采集任务。
- 当前研究结论：历史、成本、账户 funding、连续盘口/退出、权限和清算资料仍不完整，策略状态为 `Blocked / No-Go`。

## 交付约定

- 新 lesson 放在 `lessons/`，reference 放在 `reference/`，共用教学组件放在 `assets/`。
- 课程材料必须绑定真实仓库文件和实际运行结果；不能把课程阅读完成写成策略成立。
